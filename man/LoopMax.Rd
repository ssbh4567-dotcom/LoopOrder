% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LoopOrder.R
\name{LoopMax}
\alias{LoopMax}
\title{LoopMax (maximum of test statistic based) Test for Simple Loop Order Alternatives}
\usage{
LoopMax(MUU, VAR, Sam_size, significance_level, n.boot = 1e+05, seed = NULL)
}
\arguments{
\item{MUU}{A numeric vector of sample means for the groups.}

\item{VAR}{A numeric vector of biased sample variances (unrestricted MLE)
for each group.}

\item{Sam_size}{A numeric vector of sample sizes for each group.}

\item{significance_level}{A numeric value in (0,1) specifying the significance
level (e.g., \code{0.05}).}

\item{n.boot}{Number of bootstrap replications (default = \code{100000}).}

\item{seed}{Optional random seed for reproducibility.}
}
\value{
A list containing:
  \item{critical_value}{Bootstrap critical value at the specified level}
  \item{max_statistic}{Observed LoopMax test statistic}
  \item{p_value}{Bootstrap p-value}
  \item{decision}{Test decision}
}
\description{
Performs the LoopMax test for assessing equality of means across multiple
groups against the simple loop‐order alternative
\eqn{\mu_1 \le \mu_i \le \mu_k} for \eqn{i = 2, \ldots, k-1}.
The test compares the null hypothesis of equal means with an ordered
alternative in which the interior group means lie between the first and last
group means.
}
\details{
The test evaluates:
\deqn{
  H_0:\; \mu_1 = \mu_2 = \cdots = \mu_k
  \qquad \text{vs.} \qquad
  H_1:\; \mu_1 \le \mu_i \le \mu_k,\; i = 2,\ldots,k-1.
}
The LoopMax statistic is constructed by comparing each interior group mean
to the boundary groups, standardized by their respective unpooled (Welch-type)
standard errors. The maximum standardized difference is calculated for each boundary,
and the maximum of these two maximums is taken as the final test statistic.

A parametric bootstrap based on
\eqn{N(0, \sigma_i^2)} is used to obtain critical values
and p-values.
}
\examples{
M <- c(0.2, 0.8, 0.5, 1.2)
V <- c(1.5, 2.9, 4.0, 1.3)
n <- c(20, 22, 25, 18)
LoopMax(M, V, n, significance_level = 0.05, n.boot = 1000)
\donttest{
# Recommended: Use 100000
LoopMax(M, V, n, significance_level = 0.05, n.boot = 100000)
}

}
\author{
Subha Halder
}
